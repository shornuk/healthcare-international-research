{% macro linklist(params) %}
    {% set links = params.links ?? false %}
    <h3 class="text-sm font-semibold leading-6 text-slate-950 dark:text-white">{{ params.heading }}</h3>
    <ul role="list" class="mt-6 space-y-4">
    {% if links %}
        {% for link in links %}
            <li>
                <a href="{{ link.href ?? '' }}" class="text-sm leading-6 text-slate-800 dark:text-slate-300 hover:text-slate-950 dark:hover:text-white">{{ link.title ?? '' }}</a>
            </li>
        {% endfor %}
    {% endif %}
    </ul>
{% endmacro %}

<footer class="mt-12 dark:bg-slate-950" aria-labelledby="footer-heading">
    <h2 id="footer-heading" class="sr-only">Footer</h2>
    <div class="mx-auto max-w-7xl px-6 pb-8 pt-16 sm:pt-24 lg:px-8 lg:pt-32">
        <div class="xl:grid xl:grid-cols-3 xl:gap-16">
            <div class="space-y-8">
                <div class="w-full max-w-xl">
                    {% include "_includes/logo-footer.twig" %}
                </div>
                <p class="text-sm leading-6 text-slate-800 dark:text-slate-300">Elevating Natural Healthcare with Premium Wellness and Beauty Solutions.</p>
                <a class="inline-flex gap-2 text-sm" href="https://www.linkedin.com/company/healthcare-international-research"><svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" class="size-5 fill-current text-[#0077B5] dark:text-gray-600 dark:hover:text-white transition" viewBox="0 0 24 24"><path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/></svg><span class="inline-block">Find us on LinkedIn</span></a>
            </div>
    
            <div class="mt-16 grid grid-cols-2 gap-8 xl:col-span-2 xl:mt-0">
                
                <div class="md:grid md:grid-cols-2 md:gap-8">
                    <div>
                        {{ _self.linklist({
                            heading: "Our company",
                            links: companyPages
                        }) }}
                    </div>
                    <div class="mt-10 md:mt-0">
                        {{ _self.linklist({
                            heading: "Our brands",
                            links: brandPages
                        }) }}
                    </div>
                </div>
                
                <div class="md:grid md:grid-cols-2 md:gap-8">
                    <div>
                        {{ _self.linklist({
                            heading: "Information",
                            links: informationPages
                        }) }}
                    </div>
                    <div class="mt-10 md:mt-0">
                        {{ _self.linklist({
                            heading: "Legal",
                            links: legalPages
                        }) }}
                    </div>
                </div>
              
            </div>
        </div>
        
        <div class="mt-16 border-t border-slate-500/10 dark:border-white/10 pt-8 sm:mt-20 lg:mt-24 lg:flex lg:justify-between w-full">
            <p class="text-xs leading-5 text-slate-700 dark:text-gray-400">&copy; {{ now|date('Y')}} {{ siteName }}. All rights reserved. VAT number: GB341062737</p>
            <p class="text-xs leading-5 text-slate-700 dark:text-gray-400"></p>
        </div>
        
    </div>
</footer>
